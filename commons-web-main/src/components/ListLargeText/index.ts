import styled from 'styled-components';

const ListLargeText = styled.h3`
  height: 24px;
  color: #354148;
  font-family: Montserrat;
  font-size: 14px;
  font-weight: 400;
  line-height: 24px;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
`;

export default ListLargeText;
